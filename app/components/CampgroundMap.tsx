'use client'

type Props = {
  onSiteSelect?: (site: any) => void
  arrival?: string
  departure?: string
  bookedSiteIds?: string[]
}

export default function CampgroundMap({ onSiteSelect, arrival, departure, bookedSiteIds }: Props) {
  return null
}
